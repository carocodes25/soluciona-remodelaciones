# 🚀 VARIABLES DE ENTORNO PARA RENDER
# Copia estas variables en Render cuando configures el servicio

# Database (la obtienes de Render PostgreSQL)
DATABASE_URL=postgresql://usuario:password@host:5432/database

# JWT Secrets (puedes dejar estos valores)
JWT_SECRET=soluciona-jwt-secret-production-2024
JWT_REFRESH_SECRET=soluciona-refresh-secret-production-2024

# Redis (opcional - déjalo vacío si no tienes)
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=

# Server
NODE_ENV=production
PORT=10000

# CORS (la URL que te dé Vercel para el frontend)
CORS_ORIGINS=https://tu-app.vercel.app,http://localhost:3000
